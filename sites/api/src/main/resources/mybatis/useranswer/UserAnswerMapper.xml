<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace必须指向Dao接口 -->
<mapper namespace="com.gxb.sites.api.resource.useranswer.dao.UserAnswerDao">

    <insert id="save" parameterType="UserAnswer">
        INSERT INTO elective_user_answer
            (user_id, exam_id, answer, result, creation_time, deleted)
        VALUES
            (#{userid}, #{examid}, #{answer}, #{result}, now(), 0)
    </insert>

    <select id="getByUnionId" resultType="UserAnswer">
        SELECT * from elective_user_answer where unionid = #{unionId}
    </select>

    <update id="updateUserIdByUnionId">
        update elective_user_answer set user_id = #{userId} where unionid = #{unionId}
    </update>

</mapper>
